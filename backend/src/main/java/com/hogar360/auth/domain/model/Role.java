package com.hogar360.auth.domain.model;

/**
 * Roles disponibles en el sistema. Se amplía a medida que crezcan los módulos
 * (finanzas, tareas, inventario, etc.).
 */
public enum Role {
    USER,
    ADMIN
}
