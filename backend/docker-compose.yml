# docker-compose.yml
version: "3.8"
services:
  mysql:
    image: mysql:8.0
    container_name: mihogar_mysql
    environment:
      MYSQL_DATABASE: hogar_inteligente_db
      MYSQL_ROOT_PASSWORD: Terres1009.
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: